{
  "devDependencies": {
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.18",
    "tailwindcss": "^3.1.8",
    "tailwindcss-cli": "^0.1.2"
  },
  "scripts": {
    "build": "tailwindcss-cli -i src/tailwind.css -o dist/tailwind.css",
    "buildmin": "tailwindcss-cli -i src/tailwind.css -o dist/tailwind.min.css --minify"
  }
}
